import { profile } from './profile';
import { experience } from './experience';
import { organizations } from './organizations';
import { certifications, certificateImages } from './certificates';

export const portfolioData = {
  profile,
  experience,
  organizations,
  certifications
};

export { profile, experience, organizations, certifications, certificateImages };
