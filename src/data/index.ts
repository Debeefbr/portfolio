import { profile } from './profile';
import { experience } from './experience';
import { organizations } from './organizations';
import { certifications, certificateImages } from './certificates';
import { education } from './education';
import { skills } from './skills';
import { dataMindset, dataCompetencies } from './core';

export const portfolioData = {
  profile,
  experience,
  organizations,
  certifications,
  education,
  skills,
  dataMindset,
  dataCompetencies
};

export { profile, experience, organizations, certifications, certificateImages, education, skills, dataMindset, dataCompetencies };
